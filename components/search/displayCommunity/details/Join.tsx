import { FC } from "react";
import { styles } from "../../build/SearchStyles";

const Join: FC = () => {

    return (
        <button className={ styles.join_button }>
            join
        </button>
    )
}

export default Join